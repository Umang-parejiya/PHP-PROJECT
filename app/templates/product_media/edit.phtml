<?php include 'app/templates/menu.phtml'; ?>

<h2><?php echo $data->product_media_id ? 'Edit Product Media' : 'Add New Product Media'; ?></h2>

<form method="POST" action="index.php?c=product_media&a=save">
    <input type="hidden" name="product_media[product_media_id]" value="<?php echo $data->product_media_id; ?>">

    <table border="1" width="100%">
        <tr>
            <td>Product</td>
            <td>
                <select name="product_media[product_id]">
                    <option value="">Select Product</option>
                    <?php if(!empty($products)): ?>
                        <?php foreach($products as $product): ?>
                            <option value="<?php echo $product->product_id; ?>" <?php echo ($data->product_id == $product->product_id) ? 'selected' : ''; ?>>
                                <?php echo htmlspecialchars($product->name); ?>
                            </option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </td>
        </tr>
        <tr>
            <td>File Path</td>
            <td><input type="text" name="product_media[file_path]" value="<?php echo htmlspecialchars($data->file_path ?? ''); ?>"></td>
        </tr>
        <tr>
            <td>Alt Text</td>
            <td><input type="text" name="product_media[alt_text]" value="<?php echo htmlspecialchars($data->alt_text ?? ''); ?>"></td>
        </tr>
        <tr>
            <td>Sort Order</td>
            <td><input type="number" step="0.01" name="product_media[sort_order]" value="<?php echo $data->sort_order ?? 0; ?>"></td>
        </tr>
        <tr>
            <td>Is Primary</td>
            <td>
                <select name="product_media[is_primary]">
                    <option value="1" <?php echo ($data->is_primary == 1) ? 'selected' : ''; ?>>Yes</option>
                    <option value="0" <?php echo ($data->is_primary == 0) ? 'selected' : ''; ?>>No</option>
                </select>
            </td>
        </tr>
        <tr>
            <td colspan="2">
                <button type="submit">Save</button>
                <a href="index.php?c=product_media&a=list">Back</a>
            </td>
        </tr>
    </table>
</form>
